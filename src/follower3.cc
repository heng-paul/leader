#include <iostream>
#include <math.h>
#include "follower3.h"

using namespace enviro;

// Note : All implementation is in follower.h, but the Makefile
//        needs a .cc file to know what .so files to make